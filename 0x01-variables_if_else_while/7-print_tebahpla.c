#include <stdio.h>

/**
 * main - entry point
 * desc: the lowercase alphabet in reverse
 * Return: Always 0
 */
int main(void)
{
	int a = 122;

	while (n >= 97)
	{
		putchar(n);
		n--;
	}
	putchar('\n');
	return (0);
}
